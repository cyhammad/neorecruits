"use client";

/**
 * DesktopActions component handles secondary actions in the header.
 * Currently, based on the reference design, actions are moved to the TopBar
 * and hidden upon scroll to match the minimalist sticky header look.
 */
export function DesktopActions({ isScrolled }) {
  return null;
}
